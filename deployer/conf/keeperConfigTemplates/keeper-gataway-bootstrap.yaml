spring:
  application:
    version: v1
    cnname: dbus网关-v1
    name: dbus-keeper-gateway-${spring.application.version}
  cloud:
    config:
      profile: ${spring.cloud.config.profile}
      lable: ${spring.cloud.config.label}
      uri: ${spring.cloud.config.uri}